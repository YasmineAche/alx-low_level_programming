#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - main intry point
 *
 * @argc : int variable
 * @argv : pointer to list of strings
 *
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
