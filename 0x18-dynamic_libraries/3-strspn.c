#include "main.h"
/**
 * _strspn - check charactor if it's apper case
 *
 * @s: argumment
 * @accept : argument
 *
 * Return: 1 if true or else 0.
 */
unsigned int _strspn(char *s, char *accept)
{
	s++;
	accept++;
	return (0);
}
